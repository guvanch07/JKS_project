name: data
description: Data layer
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=2.2.0"

dependencies:
  flutter:
    sdk: flutter

  domain:
    path: ../domain

  rxdart: ^0.27.3
  dio: ^4.0.4
  retrofit: ^3.0.1

dev_dependencies:
  build_runner: ^2.1.7
  injectable_generator: ^1.5.3
  flutter_lints: ^1.0.0
