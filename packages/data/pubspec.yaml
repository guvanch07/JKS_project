name: data
description: Data layer
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=2.2.0"

dependencies:
  flutter:
    sdk: flutter
  domain:
    path: ../domain
  dio: ^4.0.4
  shared_preferences: ^2.0.13

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.7
  injectable_generator: ^1.5.3
  flutter_lints: ^1.0.0
  mockito: ^5.1.0
